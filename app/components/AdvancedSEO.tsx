interface AdvancedSEOProps {
  page?: string;
}

export default function AdvancedSEO(_props: AdvancedSEOProps) {
  // Intentionally left minimal to avoid duplicating JSON-LD and meta tags handled elsewhere
  return null;
}
